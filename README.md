# Forum

A small Python forum using uv, FastAPI, server-rendered Jinja2 templates,
SQLAlchemy, and SQLite.

Implementation is pending. This repository will contain application code,
ordinary functional tests, synthetic fixtures, startup/reset instructions,
and the intended user/moderator/admin access rules.
